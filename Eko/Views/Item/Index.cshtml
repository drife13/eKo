@model List<UserItem>

<table class="table">
    <tr>
        <th>Owner</th>
        <th>Title</th>
        <th>Price</th>
        <th>Description</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Item.Owner.UserName</td>
            <td>@item.Item.Title</td>
            <td>@item.Item.Price</td>
            <td>@item.Item.Description</td>
        </tr>
    }
</table>

@if (Model.Count == 0)
{
    <p>Nothing for sale!</p>
}